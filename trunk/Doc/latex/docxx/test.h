///
class Test 
{

    /** doku
      @param x ein h\"ubscher wert
     */
    Test(int x);
    ///
    ~Test();
    
};

/**
 * What kind of ATM address does the client have and how long is it?
 *
 * @return The type and length of the client's ATM address encoded
 * per RFC1577 and draft-ietf-ipatm-classic2-02.txt.
 */ 
u_int ATMNumberTypeAndLength();

main()
{
    /// eine erste variable
    int alpha;

    /// eine zweite variable
    int beta;
	
}

/** Ein kleiner Kommentar. Hier nun das URL Tag,
  \URL[text]{http://www.zib.de/Visual}, naja.*/
int bla;


/// Reentrant gethostbyname()
static struct  hostent* gethostbyname_r(const char* name, struct
			 hostent* result,
			 char* buffer, int buflen,
			 int* h_errnop);


/**@name Expliziter Eintrag
 ein expliziter eintrag mit. dokum.*/
//@{
/** Problem im parser und .*/
#define Loader (*Multiloader::getLoader())
//@}


